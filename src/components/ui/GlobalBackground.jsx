// ponytail: GlobalBackground is a standalone ambient visual — lives in components/ui
// This file is a thin re-export from the original location to maintain backward compat.
// The actual component source stays at components/GlobalBackground.jsx (no logic change needed).
export { default } from '../GlobalBackground';
